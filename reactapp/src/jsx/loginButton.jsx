import React from "react";

function LoginButton() {
  return (
    <div>
      <button
        id="register-button"
        className="btn btn-primary">
        Logga in
      </button>
    </div>
  );
}

export default LoginButton;